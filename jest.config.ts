// // jest.config.ts// import type { Config } from 'jest';// const createJestConfig = nextJest({//   dir: './',// });// const config: Config = {//   // coverageProvider: 'v8',//   // preset: 'ts-jest', // Puede ser necesario si usas TypeScript y no Babel para todo.//   // transform: {//   //   '^.+\\.[jt]sx?$': 'babel-jest', // Asegúrate de que Babel esté configurado correctamente si es tu elección.//   // },//   testEnvironment: 'jsdom',//   // moduleNameMapper: {//   //   '\\.(css|less|scss|sass)$': 'identity-obj-proxy', // Manejo de módulos CSS.//   // },//   setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'], // Aquí se carga la configuración de Jest personalizada//   testMatch: ['**/__tests__/**/*.(test|spec).[jt]s?(x)'],// };// export default createJestConfig(config);